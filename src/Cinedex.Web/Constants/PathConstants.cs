namespace Cinedex.Web.Constants;

public static class PathConstants
{
    /// <summary>
    /// The base path prefix for all API endpoints in the application.
    /// All routes will be prefixed with this value (e.g., /movie-svc/login, /movie-svc/movies).
    /// </summary>
    public const string ApiBasePath = "/movie-svc";
}
